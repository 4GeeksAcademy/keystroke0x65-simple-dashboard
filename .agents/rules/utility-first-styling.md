# Utility-First Styling

Apply component and layout styles with appropriate Tailwind utility classes.

Do not add custom CSS that duplicates, conflicts with, or redundantly replaces existing utility classes.

Use flexbox or grid utilities for layout and use breakpoint utilities when responsive behavior is needed.
