# Keep Synthetic Data Stable

Synthetic data is allowed when real data is unavailable.

Once synthetic data is added, preserve it exactly in all subsequent changes unless the user explicitly requests a data change.
